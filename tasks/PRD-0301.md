# Task PRD-0301: Define Predictor Protocol

> PRD: PRD-03 | Depends: PRD-0102  
> Files: 2 | Lines: ~120 | Time: ~45min

## What to Build
Create a predictor protocol so the attack engine can query backbones without knowing model internals.

## Files
### Create: `src/anima_def_dtp/predictors/base.py`
```python
class PredictorAdapter(Protocol):
    def predict(self, window, perturbation=None):
        ...
```

### Create: `tests/test_predictor_protocol.py`
```python
def test_dummy_predictor_matches_protocol():
    ...
```

## Verify
```bash
uv run pytest tests/test_predictor_protocol.py -v
```
