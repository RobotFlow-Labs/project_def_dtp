# Task PRD-0503: Containerize The Service

> PRD: PRD-05 | Depends: PRD-0502  
> Files: 3 | Lines: ~150 | Time: ~60min

## What to Build
Package the service in a Docker image with mounted model / dataset volumes and a simple health contract.

## Files
### Create: `Dockerfile`
```dockerfile
FROM python:3.11-slim
```

### Create: `docker-compose.yml`
```yaml
services:
  def-dtp:
    build: .
```

### Create: `tests/test_container_contract.py`
```python
def test_container_env_contract_docs_exist():
    ...
```

## Verify
```bash
uv run pytest tests/test_container_contract.py -v
docker build -t def-dtp:local .
```
