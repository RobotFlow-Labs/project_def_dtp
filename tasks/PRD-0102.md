# Task PRD-0102: Define Typed Trajectory Contracts

> PRD: PRD-01 | Depends: PRD-0101  
> Files: 3 | Lines: ~180 | Time: ~75min

## What to Build
Create typed data contracts for scenario windows, object tracks, and predictor requests so the attack engine and adapters share one canonical shape model.

## Files
### Create: `src/anima_def_dtp/types.py`
```python
from pydantic import BaseModel

class ObjectTrack(BaseModel):
    object_id: str
    observe_trace: list[list[float]]
    future_trace: list[list[float]]
```

### Create: `src/anima_def_dtp/constants.py`
```python
OBS_PRED_LENGTHS = {
    "nuscenes": {"obs": 4, "pred": 12},
    "apolloscape": {"obs": 6, "pred": 6},
}
```

### Create: `tests/test_types.py`
```python
def test_window_lengths_match_constants():
    ...
```

## Verify
```bash
uv run pytest tests/test_types.py -v
```
