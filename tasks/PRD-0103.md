# Task PRD-0103: Implement Dataset Windowing

> PRD: PRD-01 | Depends: PRD-0102  
> Files: 3 | Lines: ~220 | Time: ~90min

## What to Build
Port the repo's offline JSON framing into ANIMA helpers that generate benchmark windows and deterministic 100-sample manifests for nuScenes and Apolloscape.

## Files
### Create: `src/anima_def_dtp/data/windows.py`
```python
def input_data_by_attack_step(data, obs_length, pred_length, attack_step):
    # slice observe / future traces exactly once
    ...
```

### Create: `tests/test_windows.py`
```python
def test_attack_step_slice_lengths():
    ...
```

### Create: `artifacts/sample_manifests/.gitkeep`
```python
# placeholder
```

## Verify
```bash
uv run pytest tests/test_windows.py -v
```
