# Task PRD-0101: Correct Module Identity

> PRD: PRD-01 | Depends: None  
> Files: 3 | Lines: ~120 | Time: ~45min

## What to Build
Replace the copied `BISHAMONTEN` scaffold identity with `DEF-DTP` and correct the paper reference from `2503.15832` to `2603.26462`. This task creates a trustworthy foundation for every later artifact.

## Files
### Update: `pyproject.toml`
```toml
[project]
name = "anima-def-dtp"
description = "ANIMA DEF-DTP — DTP-Attack"
```

### Update: `configs/default.toml`
```toml
[project]
name = "anima-def-dtp"
codename = "DEF-DTP"
paper_arxiv = "2603.26462"
```

### Create: `src/anima_def_dtp/__init__.py`
```python
"""ANIMA DEF-DTP."""
from .version import __version__
```

## Verify
```bash
uv run pytest tests/test_config.py -v
```
