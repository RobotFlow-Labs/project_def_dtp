# Task PRD-0403: Generate Paper Reproduction Report

> PRD: PRD-04 | Depends: PRD-0402  
> Files: 3 | Lines: ~240 | Time: ~90min

## What to Build
Add report generation for Table I, Table II, and a parity summary. Baseline wrappers for PSO and SBA can be stubbed first, but the report interface must include them.

## Files
### Create: `src/anima_def_dtp/evaluation/baselines.py`
```python
def run_pso_baseline(...):
    ...
```

### Create: `scripts/reproduce_paper.py`
```python
def main():
    ...
```

### Create: `tests/test_report.py`
```python
def test_report_files_are_named_consistently():
    ...
```

## Verify
```bash
uv run pytest tests/test_report.py -v
uv run python scripts/reproduce_paper.py --help
```
