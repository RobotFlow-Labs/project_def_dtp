# Task PRD-0303: Port Trajectron++ Adapter And CLI

> PRD: PRD-03 | Depends: PRD-0301  
> Files: 3 | Lines: ~300 | Time: ~90min

## What to Build
Implement Trajectron++ map / non-map adapters and expose a small CLI for `normal`, `attack`, and `evaluate-case`.

## Files
### Create: `src/anima_def_dtp/predictors/trajectron.py`
```python
class TrajectronAdapter:
    def predict(self, window, perturbation=None):
        ...
```

### Create: `src/anima_def_dtp/cli.py`
```python
def main():
    ...
```

### Create: `tests/test_cli.py`
```python
def test_cli_help_smoke():
    ...
```

## Verify
```bash
uv run pytest tests/test_trajectron_adapter.py tests/test_cli.py -v
```
