# Task PRD-0203: Implement Boundary Walking Engine

> PRD: PRD-02 | Depends: PRD-0202  
> Files: 3 | Lines: ~280 | Time: ~90min

## What to Build
Implement Algorithm 1 with random adversarial initialization, orthogonal steps, forward steps, and adaptive `delta` / `epsilon`.

## Files
### Create: `src/anima_def_dtp/attack/boundary.py`
```python
class BoundaryWalker:
    def run(self, x_orig, predictor, criterion):
        ...
```

### Create: `src/anima_def_dtp/attack/engine.py`
```python
class DtpAttackEngine:
    def run_case(self, window, target_obj_id, objective):
        ...
```

### Create: `tests/test_boundary.py`
```python
def test_boundary_walker_keeps_adversarial_point():
    ...
```

## Verify
```bash
uv run pytest tests/test_boundary.py -v
```
